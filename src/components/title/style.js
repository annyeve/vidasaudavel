//Bloco de importação de componentes
import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    boxTitle:{
        alignItems: "center",
        justifyContent: "center",
        padding: 10,
    },
    textTitle:{
        color: "#000000",
        fontSize: 24,
        fontWeight: "bold",
    }
});

//Essa função é responsável pela estilização do titulo
export default styles